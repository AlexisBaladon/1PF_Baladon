import { InRangeStyleDirective } from './in-range-style.directive';

describe('InRangeStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new InRangeStyleDirective();
    expect(directive).toBeTruthy();
  });
});
